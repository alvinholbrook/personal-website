_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/
vendor/
.DS_Store

# Local Claude Code project context — contains sensitive background, must
# never be pushed to the public repo. See exclude list in _config.yml too
# (belt-and-suspenders, but this is the file that actually matters).
CLAUDE.md
CONTENT-CANDIDATES.md
